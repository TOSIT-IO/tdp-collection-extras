<?xml version="1.0" encoding="utf-8"?>
<ipset type="hash:net">
  <short>{{ short }}</short>
  {% for host in hosts %}
  <entry>{{ hostvars[host].ip | default(hostvars[host].ansible_default_ipv4.address) }}</entry>
  {% endfor %}
</ipset>
